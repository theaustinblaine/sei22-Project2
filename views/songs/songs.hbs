<h1>All Songs</h1>

<a href="songs/new">List a New Song</a>

{{#each songs}}
    <div>
        <a href="/songs/{{_id}}">{{name}}</a>
        {{writer}}
        {{publisher}}
        {{_id}}
    </div>
{{/each}}